# DefaultLongButton
import APITable from '@site/src/components/APITable';

## Rule Configuration {#configuration}

Accepted fields:

```mdx-code-block
<APITable>
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `onClick` | `MouseEventHandler<HTMLElement>` | null | Element click handler |
| `success` | `boolean` | `true` | Set success display |
| `color` | <code>'primary' \| 'dark'</code> | `'primary'` | Set the color of the button |
| `size` | <code>'small' \| 'medium' \| 'large'</code> | `'medium'` | Set the size of the button |

```mdx-code-block
</APITable>
```

Open [`http://localhost:3000/universal-components-library/DefaultLongButton`](http://localhost:3000/universal-components-library/DefaultLongButton)

```jsx title="/pages/universal-components-library/DefaultLongButton.tsx"
import React from 'react';
import Layout from '@theme/Layout';
import { DefaultLongButton } from '@314oner_npm/universal-components-library';

export default function DefaultLongButtonDocs() {
    return (
        <Layout title="DefaultLongButton" description="DefaultLongButton component">
            <DefaultLongButton onClick={function (event: React.MouseEvent<HTMLElement, MouseEvent>): void {
                throw new Error('Function not implemented.');
            }} />
        </Layout>
    );
}
```